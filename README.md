# stock tracker 
Tech stack : 
    ```
        frontend : react\n 
        backend : node\n
        db : mongodb
    ```
    cd ../frontend

# Install dependencies
npm install

# Create .env (optional)
cp .env.example .env

# Start development server
npm start